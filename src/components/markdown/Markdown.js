import ReactMarkdown from 'react-markdown'

/**
 * Unstyled Markdown Renderer. Configure the markdown renderer by adding
 * plugins/components here.
 */
const Markdown = props => <ReactMarkdown {...props} />

export default Markdown
