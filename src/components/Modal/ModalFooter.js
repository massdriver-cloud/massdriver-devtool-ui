import stylin from 'utils/stylin'
import MuiDialogActions from '@mui/material/DialogActions'

const ModalFooter = stylin(MuiDialogActions)({
  margin: 0,
  padding: '10px',
  textAlign: 'right',
  justifyContent: 'flex-end'
})

export default ModalFooter
